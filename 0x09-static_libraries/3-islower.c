#include "holberton.h"

/**
 * main - writes the character
 * Return: 0
 *
 */

int main(void)
{
	return (0);
}

/**
 * _islower - Checks whether c is a lowercase letter
 * @c: letter to return
 * Return: void
 */
int _islower(int c)
{
	if (c >= 97 && c <= 122)
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
